"use client";

import React, { FC } from "react";
import SearchContainer from "@/components/others/SearchContainer";

const Page: FC = () => {
	return (
		<div>
			<SearchContainer />
		</div>
	);
};

export default Page;
